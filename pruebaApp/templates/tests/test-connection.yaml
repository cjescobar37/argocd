apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pruebaApp.fullname" . }}-test-connection"
  labels:
    {{- include "pruebaApp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pruebaApp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
